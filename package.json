{
  "name": "bun-bagle",
  "description": "🥯 The Bun fetch mocker with a hole lot of flavor.",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun --splitting --minify --sourcemap=inline",
    "test": "bun test --coverage"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}